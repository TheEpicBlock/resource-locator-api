package nl.theepicblock.resourcelocatorapi.impl;

// import net.devtech.arrp.api.RRPCallback;
import net.minecraft.resource.ResourcePack;

import java.util.ArrayList;

public class ArrpProvider {
    public static void addPacksBeforeVanilla(CompositeResourcePack pack) {
        // var l = new ArrayList<ResourcePack>();
        // RRPCallback.BEFORE_VANILLA.invoker().insert(l);
        // l.forEach(pack::append);
    }

    public static void addPacksAfterVanilla(CompositeResourcePack pack) {
        // var l = new ArrayList<ResourcePack>();
        // RRPCallback.AFTER_VANILLA.invoker().insert(l);
        // l.forEach(pack::append);
    }
}
